name: SAM Deploy System

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  deploy-plugin:
    name: Deploy Plugin to WP.org
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy plugin folder
        uses: 10up/action-wordpress-plugin-deploy@stable
        with:
          plugin_slug: sam-scroll-master
          build_dir: plugin
          generate-zip: true
        env:
          SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
          SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Rename zip with clean version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          for file in $(find . -name "*.zip"); do
            mv "$file" "sam-scroll-master.${VERSION}.zip"
          done

      - name: Upload zip to release
        uses: softprops/action-gh-release@v2
        with:
          files: sam-scroll-master.${{ env.VERSION }}.zip
